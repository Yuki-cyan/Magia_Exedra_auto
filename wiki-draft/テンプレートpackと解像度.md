# テンプレート pack と解像度

言語：[简体中文](./首页) · [English](./Home_EN) · [日本語](./ホーム)

## aim ジャンクション

リポジトリルートの `aim/` は実フォルダではなく Windows ディレクトリジャンクションであり、`language/<言語>/<言語>_<解像度>/` 配下のいずれかの実テンプレート pack を指します。言語や解像度を切り替える際は `aim` ジャンクションの再指向のみを行い、実際のテンプレートファイルは一切動かしません。すべての実行時パス（`./aim/...`）はそのまま使用でき、ジャンクションは読み取りに対して透過です。

ジャンクションは `cmd /c mklink /J` で作成し（管理者権限不要）、`os.rmdir` で削除します（リンクのみを削除し、ターゲット内容は削除しません）。

## pack 構成

ソース pack は `language/EN/EN_2560x1440` と `language/JP/JP_2560x1440` です。派生した 720p/1080p/4K ディレクトリはコミットされた `.gitkeep` ファイルで保持され、「リストを更新」ボタンで生成されます。

`language/active.json` が最後の選択を記録しますが、実際の `aim` ターゲットが常に権威を持ちます。

## テンプレートファイル命名

テンプレートファイル名は `name` ではなく `picture` 引数を使用します。`click_item_with_result(self, picture, name)` と `find_item_with_result(...)` は `<picture>_1.png` から連番のテンプレートを検出し、最初の欠番で停止します。`name` はログラベルのみです。

番号は `_1` から開始し連続していなければなりません。例えば `crystalis/` 配下には `play_1.png`、`result_1.png` から `result_12.png`、`retry_1.png` などがあります。

同じテンプレートグループで検出されたすべてのバリアントは、1 枚の共通ゲームフレームに対して比較され、グループ全体でマッチスコアが最も高い候補だけが採用されます。番号ごとにフレームを撮り直したり、先に閾値を超えた候補を優先したりはしません。スクリーンショットとテンプレートの両方に同じ 3x3 `GaussianBlur` を適用してから、OpenCV の `TM_SQDIFF_NORMED` でマッチングします。

## 画像ごとの信頼度

`resource/template_confidence.txt` で、各論理 PNG のしきい値を個別に設定します。基本形式は `テンプレート相対パス = しきい値` で、例は `crystalis/result_1.png = 0.85` です。通常の論理パスはすべての言語と派生解像度に適用されます。必要に応じて `EN/パス`、`JP/パス`、`EN/1920x1080/パス` を追加でき、言語+解像度、言語、共通パスの順で優先されます。

判定は常に厳密な `score > しきい値` で、同値は一致しません。保存後は次回の認識でホットリロードされ、再起動は不要です。欠落、重複、数値でない値、0.0-1.0 の範囲外は認識を拒否し、グローバル値にはフォールバックしません。タスク開始時に、アクティブ pack の全 PNG にしきい値があることを検証します。テンプレートの追加、削除、改名と同時に TXT も更新してください。

## 解像度スケーリング

2K（2560x1440）を唯一の標準ソースとし、倍率で他解像度を派生します：

| 解像度 | 倍率 | 備考 |
|:------:|:----:|:-----|
| 1280x720 | 0.5x | ダウンサンプリング、品質良好 |
| 1920x1080 | 0.75x | ダウンサンプリング、品質良好 |
| 3840x2160 | 1.5x | アップサンプリング、非整数倍、ややぼやける |

ダウンサンプリングした 720p/1080p pack はアップサンプリングした 4K pack より高品質ですが、空のまま使えないよりはマシです。

スケーリングには `tools/ImageMagick/magick.exe` を使用し、ImageMagick の `-filter Triangle -resize` でファイルごとに処理しながら、ソース pack のサブディレクトリ構造をターゲット pack に保持します。`mogrify -path` はサブディレクトリをフラット化して `<dirpath>_N.png` のグループ構造を壊すため使用しません。

現在のスケーリングレシピは recipe v2 です。ソース SHA-256、レシピ/ツールのフィンガープリント、ターゲット SHA-256 のすべてが manifest と一致する場合に限り生成をスキップします。いずれかが変化すれば再生成されるため、破損したターゲットや古いターゲットが再利用されることはありません。アプリのアップグレード後は「リストを更新」を実行し、現在のレシピで派生 pack を再構築してください。

## pack の可用性

pack が利用可能な条件は、その安全な実ディレクトリツリーに構造、CRC、Pillow デコードチェックを通過する非 reparse の PNG が1枚以上あることです。完全な PNG 検証は chunk 長、CRC、IHDR/IEND をチェックし、切り詰められたファイルが完全な pack と誤判定されるのを防ぎます。

ドロップダウン選択は `QComboBox.activated` で自動切り替えします。プログラム的な更新は切り替えをトリガーしません。空/無効な pack は `（空）` と表示されます。

## 実行時の解像度マッチング

起動時にクライアント領域の幅と高さ（タイトルバーや枠を含まない実際のレンダリング領域）をそれぞれ比較し、`|detected - expected| <= max(40px, expected*10%)` を許容します。これによりタイトルバー/枠/DPI スケーリングによる小さな偏差を許容し、厳密な一致を要求しません。

未知または不一致の解像度は起動をブロックします。スケーリング座標アクションは未知の pack 倍率の場合、2K を推測せず拒否します。

認識処理は画面に表示されているゲームウィンドウをキャプチャするため、自動操作中はゲーム画面を表示したまま、ほかのウィンドウに隠れないようにしてください。

## 重要なルール

- `aim/` を手動でマージ/リネームしたり、派生テンプレートを真のソースとして編集しないでください。LV4 などの新規テンプレートやバリアントは 2K ソース pack のみに追加し、「リストを更新」で他の解像度を派生してください
- 空 pack の `.gitkeep` プレースホルダを削除しないでください。欠落した pack ディレクトリはスケーラによって再作成されません
- `aim/`、`active.json`、派生 PNG、`.source_hashes.json` はすべて gitignore 対象です（`.gitignore` 参照）
- コード内にグローバル信頼度を追加せず、全テンプレートのしきい値を `resource/template_confidence.txt` で管理してください

> このページは AI による翻訳であり、曖昧さや不正確な箇所が含まれる可能性があります。正確な情報は [简体中文 Wiki](./模板包与分辨率) を参照してください。
