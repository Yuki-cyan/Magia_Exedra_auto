# 開発規約

言語：[简体中文](./首页) · [English](./Home_EN) · [日本語](./ホーム)

## コードスタイル

- コードコメントと GUI 文章は中国語優先です
- `README.md` が権威あるソースです。`README_EN.md` と `README_JP.md` は維持された翻訳です
- デフォルトは ASCII とし、明確な理由がありファイルが既にその文字セットである場合のみ非 ASCII やその他の Unicode を導入します
- コードが自明でない場合のみ簡潔なコメントを追加し、「変数に値を代入する」ような空の叙述を避けます

## 主要な戻り値の規約

- **テンプレートアクションの戻り値は `2`/`1` であり、ブール値ではありません。** `2` は成功/発見/クリック済み、`1` は未発見/再試行継続/キャンセルを意味します。`find_win()` が例外で、ジオメトリタプルまたは `None` を返します
- **テンプレートファイル名は `name` ではなく `picture` 引数を使用します。** `click_item_with_result(self, picture, name)` と `find_item_with_result(...)` は `<picture>_1.png` から連番のバリアントを検出し、最初の欠番で停止します。`name` はログラベルのみです。番号は `_1` から開始し連続している必要があります
- 同じテンプレートグループのすべてのバリアントを同一のキャプチャフレームと比較し、グループ全体でマッチスコアが最も高い候補だけを操作対象にしてください。テンプレートごとの再キャプチャや検出順での早期リターンは禁止です
- スクリーンショットとテンプレートの両方に同じ 3x3 `GaussianBlur` を適用してから `TM_SQDIFF_NORMED` で比較します。勝者となった各 PNG は `resource/template_confidence.txt` から解決された個別しきい値を厳密に上回る必要があります。しきい値と同値の場合はマッチせず、しきい値が欠落または無効な場合は認識を拒否します
- 0/300/600 ms の確認フレームはすべて完全なテンプレートグループを再比較し、最初のフレームの勝者だけを追跡してはいけません。しきい値の解決には worker 起動時に固定された `expected_pack` を再利用します

## 実行/停止状態

- 実行/停止状態は worker ごとです。`_running()` は `_active and not _stop_event.is_set()` です
- `worker.stop()` を使用してください。グローバルな `guaji` フラグを再導入しないでください
- `start()` は旧スレッドがまだ実行中の場合は再起動を拒否します

## LP 回復値

- LP 回復値は drinks + 1 です。GUI は Link Raid で 0-10、Crystalis で 0-8 を表示しますが、`shown + 1` を渡します
- 内部 `1` は薬を使用しないことを意味し、最初の枯渇時に停止します

## ログ

- 実行時のデバッグ出力には `print()` ではなく `logging.getLogger(__name__)` を使用します
- GUI ログハンドラでは実行時に DEBUG/INFO/WARNING/ERROR/CRITICAL を選択できます。新規インストールは INFO がデフォルトで、同じ選択が `logs/` のファイルハンドラにも適用されます
- コンソールがある場合のデフォルトは WARNING で、環境変数 `MAGIA_LOG_LEVEL` で上書きできます。`--windowed` ビルドにはコンソールがないため、stderr がなくても安全にスキップし、GUI の起動に影響を与えてはいけません
- worker のユーザー向け状態メッセージは引き続き `signal.emit` で送信し、GUI に常に表示します。メインウィンドウは GUI 重複防止マーカー付きの `magia.runtime` INFO としてファイルへミラーし、新しいチャネルで重複表示を発生させてはいけません
- ログ保持はデフォルト7日、1～365日です。クリーンアップは厳密な名前の期限切れ Magia ログだけを削除し、現在のファイルハンドラのログ族と無関係なファイルを保持する必要があります
- 各ファイルの `if __name__ == "__main__"` 診断ブロックは `print` を維持して構いません。これは CLI 直読出力であり、実行時ノイズではありません

## GUI パラメータ

- GUI パラメータはレジストリ駆動です。モジュールレベルのパラメータグローバルはありません
- `ParamSpec.kind` は `choice`、`ordered_multi_choice`、`bool`、`lp_recover`、`int` をサポートします。新しい種類では永続化値の正規化も実装してください
- 全登録パラメータは安定した `WorkerMeta.name` ごとに即時かつアトミックに保存します。`lp_recover` は GUI 表示値、有序複数選択は空でない重複なし優先順位リストを保存します
- `required_templates` のパスには動的プレースホルダを使用できますが、各プレースホルダ名はその worker で宣言済みの `ParamSpec.key` と一致する必要があります
- 新しいコントロールタイプを追加するには、`registry.py` に `kind` を追加し、`main.py` の `_build_param_widget` に対応する分岐を追加します

## 新しい周回モードの追加

1. `src/workers/` 配下に `BaseWorker` サブクラスファイルを作成します
2. `@register` でデコレートし、表示名、開始画面のヒント、パラメータリスト、必須テンプレートを宣言します
3. `src/workers/__init__.py` にインポート行を1行追加します
4. `main.py` の GUI に対応するボタンとパラメータコントロールが自動的に表示されます。GUI コードの変更は不要です

worker は引数なしで構築可能であり、`run()` を `_run_safely()` 経由でルーティングする必要があります。

GUI の停止操作とアプリ終了は必ず `worker.stop()` を呼び、`_finish()` を直接呼んではいけません。そうしないと手動停止が自動終了として誤報されます。`_emit_major_event()` は同じ実行の重大イベントを記録し、`_run_safely()` は手動ではなく他の重大イベントもない場合だけ `worker_auto_ended` を追加します。

Server酱 のチャネル設定は常に1～2個に制限します。GUI は3個目の選択を拒否し、`app_settings` も永続化境界で旧設定や手動編集された上限超過値を切り詰める必要があります。

## ユーザー入力保護と待機時の回復処理

- キーボード入力、実際のマウスボタン操作、または累積したマウス移動を検出すると自動操作を一時停止し、ユーザー入力がない状態が 5 秒間続いてから再開します
- 新しい PyAutoGUI 入力経路では、必ず先に `_wait_for_user_idle()` を呼び出し、`_automation_input()` コンテキスト内で自動マウス操作を実行し、成功後に最後の自動操作位置を記録してください
- 最後の自動操作位置はゲームクライアント基準の相対座標で保存します。ウィンドウ移動時は再計算し、クライアントサイズの変更または範囲外座標では回復クリックを拒否します
- 1 回のクリックテンプレート待機中は、未検出状態が 5 秒連続するたびにゲームのクライアント領域を 2 秒間観察します。変化したピクセルが 50% を超える場合は戦闘アニメーションとみなし、その回の回復処理だけをスキップします。それ以外の場合は、最後の安全な自動操作位置で回復クリックを 1 回実行します
- この 5 秒周期はテンプレートの検出成功、待機のタイムアウト、またはタスクのキャンセルまで継続しなければなりません。1 回だけの回復チェックとして実装してはいけません

## DPI に敏感なインポート順序

`src.workers` は遅延インポートのままにしてください。起動は `QApplication(...) -> mywindow() -> get_worker_registry()` の順序を維持する必要があります。workers を先にインポートすると PyAutoGUI が読み込まれ、Qt が Windows DPI awareness を設定できなくなる可能性があります。

## テンプレート pack のルール

- `aim/` は実行時の Windows ディレクトリジャンクションであり、実テンプレートフォルダではありません
- `aim/` を手動でマージ/リネームしたり、派生テンプレートを真のソースとして編集しないでください。新規バリアントは 2K ソース pack に追加して再生成してください
- 空 pack の `.gitkeep` プレースホルダを削除しないでください。欠落した pack ディレクトリはスケーラによって再作成されません
- 言語を追加する際は `language_switcher.LANG_LABELS` にエントリを追加してください。GUI が自動的に読み取ります

## ゲームタイトル

ゲームウィンドウのタイトルは `MadokaExedra` としてハードコードされています。変更する場合は `main.py`、`click_action.py`、`click_behavior.py` を更新するか、まず一元化する必要があります。

## Git とローカル産物

- `.gitignore` は `aim/`、`language/active.json`、ルートの `settings.json`、`USER_REQUIREMENTS.txt`、`__pycache__/`、`*.pyc`、`logs/` を無視します
- Server酱 SendKey はローカル `settings.json` だけに保存し、ログ、例外テキスト、テストフィクスチャ、要件台帳、文書、リリース成果物へ記録してはいけません
- `.gitignore` は派生 PNG、`.source_hashes.json`、`.release-venv-*/`、`build/`、`dist/`、生成された `*.spec` ファイルも無視し、コミット済みの `.gitkeep` pack プレースホルダは維持します
- `tools/ImageMagick/` はコミットされており、リリースパッケージに必要です
- リポジトリには `main`、`beta` などのブランチがあります。現在のブランチのみから対象ブランチやリリースチャネルを推測しないでください
- コミットメッセージは簡潔な主題と本文を持つ平易な中国語です

## チェックリスト

標準ライブラリの回帰テストは `tests/` にあります。lint/typecheck 設定と CI はまだありません。変更後は該当する非ゲームチェックを実行してください：

- `python -B -m unittest discover -s tests -v`
- Python コンパイル/インポートチェック
- worker レジストリとテンプレート検証
- 更新 ZIP/展開チェック
- ロック/アップデータチェック
- AMD64 PE 検証
- パッケージ化後のスモークスタート

> このページは AI による翻訳であり、曖昧さや不正確な箇所が含まれる可能性があります。正確な情報は [简体中文 Wiki](./开发约定) を参照してください。
