# 模板包与分辨率

语言：[简体中文](./首页) · [English](./Home_EN) · [日本語](./ホーム)

## aim 联接

根目录下的 `aim/` 不是真实文件夹，而是一个 Windows 目录联接（junction），指向 `language/<语言>/<语言>_<分辨率>/` 里的某个真实模板包。切换语言/分辨率时，只需把 `aim` 这个 junction 重新指向目标 pack，真正的模板文件一行都不动。所有运行时路径（`./aim/...`）照常使用，联接对读取透明。

junction 用 `cmd /c mklink /J` 创建（不需要管理员权限），用 `os.rmdir` 删除（只删链接，不删目标内容）。

## pack 组织

源 pack 是 `language/EN/EN_2560x1440` 和 `language/JP/JP_2560x1440`。派生的 720p/1080p/4K 目录由提交的 `.gitkeep` 文件保留，并由「刷新列表」填充。

`language/active.json` 记录上次选择，但实际的 `aim` 目标始终是权威来源。

## 模板文件命名

模板文件使用 `picture` 参数命名，不是 `name`。`click_item_with_result(self, picture, name)` 和 `find_item_with_result(...)` 会从 `<picture>_1.png` 开始连续发现编号模板，直到遇到第一个缺失的编号。`name` 只是日志标签。

编号必须从 `_1` 开始且连续。例如 `crystalis/` 下的 `play_1.png`、`result_1.png` 到 `result_12.png`、`retry_1.png` 等。

同一模板组的所有已发现变体共享同一帧游戏截图进行比较，最终只采用整组中匹配分数全局最高的候选，不按编号逐张截图或优先点击先匹配者。截图和模板都会先应用相同的 3x3 `GaussianBlur`，再使用 OpenCV `TM_SQDIFF_NORMED` 匹配。

## 逐图片置信度

`resource/template_confidence.txt` 为每个逻辑 PNG 单独配置匹配阈值。基本格式是 `相对模板路径 = 阈值`，例如 `crystalis/result_1.png = 0.85`。同一逻辑路径默认适用于全部语言和派生分辨率，也可增加 `EN/路径`、`JP/路径` 或 `EN/1920x1080/路径` 进行更精确的覆盖；优先级是语言+分辨率、语言、通用路径。

匹配条件始终是严格 `score > 阈值`，相等不算命中。文件保存后会在下一次识图时自动重载，无需重启。缺项、重复项、非数字或超出 0.0-1.0 的值会拒绝识别；程序不会回退到全局默认值。开始挂机前会检查当前 pack 的每张 PNG 都能解析到阈值。新增、删除或改名模板时必须同步修改该 TXT。

## 分辨率缩放

以 2K（2560x1440）为唯一标准源，按倍率派生其它分辨率：

| 分辨率 | 倍率 | 说明 |
|:------:|:----:|:-----|
| 1280x720 | 0.5x | 下采样，质量好 |
| 1920x1080 | 0.75x | 下采样，质量好 |
| 3840x2160 | 1.5x | 上采样，非整数倍，模板略糊 |

下采样的 720p/1080p pack 质量优于上采样的 4K pack，但总比空着没法用强。

实际缩放使用 `tools/ImageMagick/magick.exe`，以 ImageMagick 的 `-filter Triangle -resize` 逐张处理，并保留源 pack 的子目录结构写到目标 pack。`mogrify` 的 `-path` 会把子目录拍平，破坏 `<dirpath>_N.png` 的分组结构，因此不使用该方式。

当前缩放配方版本为 recipe v2。只有源文件 SHA-256、配方/工具指纹和目标文件 SHA-256 全部与 manifest 相符时才跳过生成；任一条件变化都会重新生成，避免损坏或陈旧目标被误用。程序升级后应点击「刷新列表」，按当前配方重建派生 pack。

## pack 可用性

一个 pack 可用的条件是其安全的真实目录树中至少有一张非 reparse 的 PNG 通过结构、CRC 和 Pillow 解码检查。完整 PNG 校验检查 chunk 长度、CRC、IHDR/IEND，避免截断文件让 pack 被误判为完整。

下拉框选择会在 `QComboBox.activated` 时自动切换；程序化刷新不会触发切换。空/无效 pack 显示「（空）」。

## 运行时分辨率匹配

启动时比较客户区宽高（实际渲染区，不含标题栏/边框），各自允许 `|detected - expected| <= max(40px, expected*10%)`，兼容标题栏/边框/DPI 缩放带来的小幅偏差，不要求严格相等。

未知/不匹配的分辨率会阻断启动。缩放坐标动作在未知 pack 因子时会拒绝执行，而不是猜测 2K。

识别依赖可见窗口截图，运行期间游戏画面必须保持可见且不被其它窗口遮挡。

## 重要规则

- 不要手动合并/重命名 `aim/` 或把派生模板当作真源编辑。LV4 等新增模板或变体只应加入 2K 源 pack，再通过「刷新列表」派生其它分辨率
- 不要删除空 pack 的 `.gitkeep` 占位。缺失的 pack 目录不会被缩放器重建
- `aim/`、`active.json`、派生 PNG、`.source_hashes.json` 均不入库，见 `.gitignore`
- 不要在代码中新增全局置信度；所有模板阈值统一维护在 `resource/template_confidence.txt`
